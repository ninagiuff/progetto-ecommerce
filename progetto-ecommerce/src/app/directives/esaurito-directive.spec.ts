import { EsauritoDirective } from './esaurito-directive';

describe('EsauritoDirective', () => {
  it('should create an instance', () => {
    const directive = new EsauritoDirective();
    expect(directive).toBeTruthy();
  });
});
